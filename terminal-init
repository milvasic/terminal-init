#!/usr/bin/env bash

set -euo pipefail

VERSION="0.1.0"

# Verify apt is available (Debian/Ubuntu only)
if ! command -v apt >/dev/null 2>&1; then
	echo "[ERROR] This script requires apt. Debian/Ubuntu only." >&2
	exit 1
fi

# Trap errors per command without stopping the whole script
report_error() {
	echo -e "\n[ERROR] $1 failed. Continuing...\n"
}

USER_HOME="$HOME"
ZSHRC="$USER_HOME/.zshrc"
SPACESHIP_CFG="$USER_HOME/.config/.spaceshiprc.zsh"

# ---------------------------------------------------------------------
# update: re-write config files only (steps 4 & 5)
# ---------------------------------------------------------------------
update() {
	# ------------------------------------------------------------------
	# 4. Write .zshrc
	# ------------------------------------------------------------------
	echo "[STEP] Creating ~/.zshrc ..."

	if [ -f "$ZSHRC" ]; then
		cp "$ZSHRC" "${ZSHRC}.bak"
		echo "[INFO] Backed up existing .zshrc to ${ZSHRC}.bak"
	fi

	if cat >"$ZSHRC" <<'EOF'; then
export ZSH="$HOME/.oh-my-zsh"
export PATH=/usr/sbin:$PATH
export ZSH_COMPDUMP=$ZSH/cache/.zcompdump-$HOST
export SPACESHIP_CONFIG="$HOME/.config/.spaceshiprc.zsh"
 
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
 
# alias python="python3"
ZSH_THEME=""
plugins=(git docker docker-compose)
 
source $ZSH/oh-my-zsh.sh
 
### Added by Zinit's installer
if [[ ! -f $HOME/.local/share/zinit/zinit.git/zinit.zsh ]]; then
    print -P "%F{33} %F{220}Installing %F{33}ZDHARMA-CONTINUUM%F{220} Plugin Manager …%f"
    command mkdir -p "$HOME/.local/share/zinit" && command chmod g-rwX "$HOME/.local/share/zinit"
    command git clone https://github.com/zdharma-continuum/zinit "$HOME/.local/share/zinit/zinit.git" && \
        print -P "%F{33} %F{34}Installation successful.%f%b" || \
        print -P "%F{160} The clone has failed.%f%b"
fi
 
source "$HOME/.local/share/zinit/zinit.git/zinit.zsh"
autoload -Uz _zinit
(( ${+_comps} )) && _comps[zinit]=_zinit
 
zinit light-mode for \
    zdharma-continuum/zinit-annex-as-monitor \
    zdharma-continuum/zinit-annex-bin-gem-node \
    zdharma-continuum/zinit-annex-patch-dl \
    zdharma-continuum/zinit-annex-rust
 
zinit light spaceship-prompt/spaceship-prompt
zinit light zdharma-continuum/fast-syntax-highlighting
zinit light zsh-users/zsh-autosuggestions
zinit light zsh-users/zsh-completions
 
alias clear-shell-history="echo '' > ~/.zsh_history"
alias clear-journal="sudo journalctl --rotate && sudo journalctl --vacuum-time=1s"
alias disk-usage="cd / && sudo ncdu && cd - > /dev/null"
alias dps="docker ps -a --format \"table {{.Names}}\t{{.State}}\t{{.Status}}\t{{.Size}}\""
alias dpsp="docker ps -a --format \"table {{.Names}}\t{{.State}}\t{{.Status}}\t{{.Size}}\t{{.Ports}}\""
alias listen-ports="sudo lsof -i -P -n | grep LISTEN"

command -v motd >/dev/null && motd
EOF
		echo "[OK] .zshrc created."
	else
		report_error ".zshrc creation"
	fi

	# ------------------------------------------------------------------
	# 5. Create ~/.config/.spaceshiprc.zsh
	# ------------------------------------------------------------------
	echo "[STEP] Creating spaceship configuration..."

	mkdir -p "$USER_HOME/.config"

	if [ -f "$SPACESHIP_CFG" ]; then
		cp "$SPACESHIP_CFG" "${SPACESHIP_CFG}.bak"
		echo "[INFO] Backed up existing .spaceshiprc.zsh to ${SPACESHIP_CFG}.bak"
	fi

	if cat >"$SPACESHIP_CFG" <<'EOF'; then
SPACESHIP_USER_SHOW=always
SPACESHIP_HOST_SHOW=always
SPACESHIP_TIME_SHOW=true
SPACESHIP_CHAR_SYMBOL_ROOT="# "
 
SPACESHIP_HG_SHOW=false
SPACESHIP_HG_BRANCH_SHOW=false
SPACESHIP_HG_STATUS_SHOW=false
SPACESHIP_PACKAGE_SHOW=false
SPACESHIP_RUBY_SHOW=false
SPACESHIP_ELM_SHOW=false
SPACESHIP_XCODE_SHOW_LOCAL=false
SPACESHIP_SWIFT_SHOW_LOCAL=false
SPACESHIP_GOLANG_SHOW=false
SPACESHIP_PHP_SHOW=false
SPACESHIP_RUST_SHOW=false
SPACESHIP_HASKELL_SHOW=false
SPACESHIP_JULIA_SHOW=false
SPACESHIP_DOCKER_CONTEXT_SHOW=false
SPACESHIP_AWS_SHOW=false
SPACESHIP_VENV_SHOW=false
SPACESHIP_CONDA_SHOW=false
SPACESHIP_PYTHON_SHOW=false
SPACESHIP_EMBER_SHOW=false
SPACESHIP_KUBECTL_SHOW=false
SPACESHIP_KUBECTL_CONTEXT_SHOW=false
SPACESHIP_TERRAFORM_SHOW=false
SPACESHIP_BATTERY_SHOW=false
SPACESHIP_VI_MODE_SHOW=false
SPACESHIP_JOBS_SHOW=false
SPACESHIP_EXIT_CODE_SHOW=false
 
SPACESHIP_PROMPT_ORDER=(host time user dir git node dotnet docker exec_time char)
EOF
		echo "[OK] Spaceship config created."
	else
		report_error "spaceship config creation"
	fi
}

# ---------------------------------------------------------------------
# ensure_tools: full first-time setup
# ---------------------------------------------------------------------
ensure_tools() {
	echo "=== terminal-init ensure_tools ==="

	# ------------------------------------------------------------------
	# 1. Install required packages
	# ------------------------------------------------------------------
	echo "[STEP] Checking base packages (curl git zsh ncdu)..."

	missing_pkgs=""
	command -v curl >/dev/null 2>&1 || missing_pkgs="$missing_pkgs curl"
	command -v git >/dev/null 2>&1 || missing_pkgs="$missing_pkgs git"
	command -v zsh >/dev/null 2>&1 || missing_pkgs="$missing_pkgs zsh"
	command -v ncdu >/dev/null 2>&1 || missing_pkgs="$missing_pkgs ncdu"

	if [ -n "$missing_pkgs" ]; then
		echo "[INFO] Missing packages:$missing_pkgs"
		if sudo apt update && sudo apt install -y $missing_pkgs; then
			echo "[OK] Packages installed."
		else
			report_error "Package installation"
		fi
	else
		echo "[SKIP] All base packages already installed."
	fi

	# ------------------------------------------------------------------
	# 2. Install oh-my-zsh
	# ------------------------------------------------------------------
	echo "[STEP] Installing oh-my-zsh..."

	if [ -d "$HOME/.oh-my-zsh" ]; then
		echo "[SKIP] oh-my-zsh already installed."
	elif RUNZSH=no CHSH=no sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"; then
		echo "[OK] oh-my-zsh installed."
	else
		report_error "oh-my-zsh installation"
	fi

	# ------------------------------------------------------------------
	# 3. Set ZSH as default shell
	# ------------------------------------------------------------------
	echo "[STEP] Setting zsh as default shell..."

	if [ "$SHELL" = "$(command -v zsh)" ]; then
		echo "[SKIP] zsh is already the default shell."
	elif chsh -s "$(command -v zsh)"; then
		echo "[OK] Shell changed to zsh."
	else
		report_error "chsh command"
	fi

	# ------------------------------------------------------------------
	# 4 & 5. Write config files
	# ------------------------------------------------------------------
	update

	# ------------------------------------------------------------------
	# 6. Install motd
	# ------------------------------------------------------------------
	echo "[STEP] Installing motd..."

	if command -v motd >/dev/null 2>&1; then
		echo "[SKIP] motd already installed."
	elif sh -c "$(curl -fsSL https://raw.githubusercontent.com/milvasic/motd/refs/heads/main/install.sh)" -- --yes; then
		echo "[OK] motd installed."
	else
		report_error "motd installation"
	fi

	# ------------------------------------------------------------------
	# 7. Install mcli
	# ------------------------------------------------------------------
	echo "[STEP] Installing mcli..."

	if command -v mcli >/dev/null 2>&1; then
		echo "[SKIP] mcli already installed."
	elif sh -c "$(curl -fsSL https://raw.githubusercontent.com/milvasic/mcli/refs/heads/main/install.sh)" -- --yes; then
		echo "[OK] mcli installed."
	else
		report_error "mcli installation"
	fi

	echo -e "\n=== Installation complete ==="
	echo "Restart your terminal or log out/in to activate zsh."
}

usage() {
	cat <<'EOF'
Usage: terminal-init <command>

Commands:
  ensure-tools  Full first-time setup (packages, OMZ/Zinit, shell, configs)
  update         Re-write ~/.zshrc and ~/.config/.spaceshiprc.zsh only
  version     Print version
  help        Show usage
EOF
}

case "${1:-help}" in
ensure-tools) ensure_tools ;;
update) update ;;
version | -v) echo "terminal-init $VERSION" ;;
help | -h | *)
	usage
	exit 1
	;;
esac
